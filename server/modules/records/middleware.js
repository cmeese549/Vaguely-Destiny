(() => {
    'use strict';

    const axios = require('axios');

    module.exports = {
        getAllRecordData
    }

    const bungieConfig = require('../../config/bungie/config');

    function getAllRecordData(req, res, next) {
        
    }

})();